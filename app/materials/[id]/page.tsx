import React from 'react';

// Teachers can review the materials here and also edit or delete them.

const Page = ({ id }: { id: string }) => {
  return <div>Material {id}</div>;
};

export default Page;
